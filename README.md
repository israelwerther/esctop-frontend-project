# esctop-frontend-project

Criando o backend
$ npm init -y
$ npm i json-server

Criar arquivo db.json

Altere o script do packge.json > "scripts": {
    "start": "json-server --watch db.json --port 3001"
},

$ npm start

